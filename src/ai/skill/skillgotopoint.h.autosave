#ifndef SKILLGOTOPOINT_H
#define SKILLGOTOPOINT_H

#include "position.h"
#include "skill.h"
#include "agent.h"

class SkillGotoPoin : public Skill
{
public:
    explicit SkillGotoPoint(int id, QString name, QObject *parent = 0);
    virtual bool execute(Agent* agent, RobotCommand& rc);

protected:
    Position
};

#endif // SKILLGOTOPOINT_H
